#!/bin/sh
set -eu
if ! command -v gitleaks >/dev/null 2>&1; then
    echo "Gitleaks is required for commits. Install it and add it to PATH." >&2
    exit 1
fi
exec gitleaks git --pre-commit --staged --redact=100 --no-banner --ignore-gitleaks-allow
